import React from "react";
import useStyles from "./styles";

const HomePage = () => {
  const classes = useStyles();

  return (
    <React.Fragment>

    </React.Fragment>
  );
};

export default HomePage;
